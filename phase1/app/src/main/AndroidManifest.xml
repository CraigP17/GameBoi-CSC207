<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gameboi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RpsFinalLostDisplay"></activity>
        <activity android:name=".Leaderboard" />
        <activity android:name=".RpsFinalWonDisplay" />
        <activity android:name=".tryAgainDisplay" />
        <activity android:name=".RpsLostRoundDisplay" />
        <activity
            android:name=".RpSWonRoundDisplay"
            android:parentActivityName=".RockPaperScissors">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".RockPaperScissors" />
        </activity>
        <activity android:name=".RockPaperScissors" />
        <activity android:name=".SimonGame" />
        <activity android:name=".MathGame" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>